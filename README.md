# NFL 戦術シミュレーター (Flask)

簡単な Flask アプリです。4つのスタッツを入力して推奨パス/ラン割合を計算します。

セットアップ・実行:

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

ブラウザで http://127.0.0.1:5000/ を開いてください。
